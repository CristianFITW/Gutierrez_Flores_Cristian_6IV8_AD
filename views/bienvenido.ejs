<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bienvenido</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap');
        
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.7)), 
                        url('https://images.unsplash.com/photo-1540747913346-19e32dc3e97e?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1920&q=80') no-repeat center center;
            background-size: cover;
            color: #fff;
            text-align: center;
        }

        .welcome-container {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border-radius: 25px;
            padding: 3rem 4rem;
            width: 90%;
            max-width: 700px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.2);
            animation: fadeIn 0.8s ease-out;
            margin-bottom: 1.5rem;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.95); }
            to { opacity: 1; transform: scale(1); }
        }

        h1 {
            font-size: 2.8rem;
            margin-bottom: 1rem;
            color: #fff;
            text-shadow: 0 2px 8px rgba(0, 0, 0, 0.4);
            position: relative;
            display: inline-block;
        }

        h1::after {
            content: '';
            position: absolute;
            width: 50%;
            height: 3px;
            bottom: -10px;
            left: 25%;
            background: linear-gradient(to right, transparent, rgba(3, 201, 169, 0.8), transparent);
            border-radius: 3px;
        }

        .welcome-message {
            font-size: 1.2rem;
            margin-bottom: 2.5rem;
            color: rgba(255, 255, 255, 0.9);
            line-height: 1.6;
        }

        .username {
            font-weight: 600;
            color: #03c9a9;
            text-shadow: 0 0 10px rgba(3, 201, 169, 0.3);
        }

        .actions {
            display: flex;
            justify-content: center;
            gap: 1.5rem;
            margin-top: 2rem;
            flex-wrap: wrap;
        }

        .action-btn {
            padding: 14px 28px;
            border-radius: 12px;
            font-size: 1rem;
            font-weight: 500;
            text-decoration: none;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            backdrop-filter: blur(5px);
            display: flex;
            align-items: center;
            justify-content: center;
            min-width: 180px;
        }

        .logout-btn {
            background: linear-gradient(45deg, rgba(231, 76, 60, 0.8), rgba(192, 57, 43, 0.8));
            color: white;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .video-btn {
            background: linear-gradient(45deg, rgba(41, 128, 185, 0.8), rgba(52, 152, 219, 0.8));
            color: white;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .home-btn {
            background: linear-gradient(45deg, rgba(155, 89, 182, 0.8), rgba(142, 68, 173, 0.8));
            color: white;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .action-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
        }

        .logout-btn:hover {
            background: linear-gradient(45deg, rgba(231, 76, 60, 1), rgba(192, 57, 43, 1));
        }

        .video-btn:hover {
            background: linear-gradient(45deg, rgba(41, 128, 185, 1), rgba(52, 152, 219, 1));
        }

        .home-btn:hover {
            background: linear-gradient(45deg, rgba(155, 89, 182, 1), rgba(142, 68, 173, 1));
        }

        .action-btn i {
            margin-right: 8px;
            font-size: 1.1rem;
        }

        .additional-actions {
            margin-top: 1.5rem;
        }

        @media (max-width: 768px) {
            .welcome-container {
                padding: 2.5rem 2rem;
            }
            
            h1 {
                font-size: 2.2rem;
            }
            
            .actions {
                flex-direction: column;
                gap: 1rem;
            }
            
            .action-btn {
                width: 100%;
            }
        }

        @media (max-width: 480px) {
            h1 {
                font-size: 1.8rem;
            }
            
            .welcome-message {
                font-size: 1rem;
            }
        }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <div class="welcome-container">
        <h1>Bienvenido, <span class="username"><%= nombreUsuario %></span>!</h1>
        <p class="welcome-message">Has iniciado sesión correctamente en nuestro sistema de gestión de jugadores.</p>
        
        <div class="actions">
            <a href="/logout" class="action-btn logout-btn">
                <i class="fas fa-sign-out-alt"></i> Cerrar sesión
            </a>
            <a href="https://www.youtube.com/watch?v=1WKN8uwct74" target="_blank" class="action-btn video-btn">
                <i class="fas fa-play"></i> Ver Video
            </a>
        </div>
    </div>

    <div class="additional-actions">
        <a href="/" class="action-btn home-btn">
            <i class="fas fa-home"></i> Volver al Inicio
        </a>
    </div>
</body>
</html>